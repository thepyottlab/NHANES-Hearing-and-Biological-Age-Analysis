# Install
1. git clone https://github.com/TomNaber/NHANES_Analysis.git
2. Open `NHANES Analysis.Rproj`
3. In R terminal, run `install.packages("renv")`  # if not already installed
4. In R terminal, run `renv::restore()`
